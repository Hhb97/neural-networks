# 神经网络进阶：分类、激活函数与优化器（第二步基本功）

> 承接上一篇《[从机器学习到神经网络：零基础入门](从机器学习到神经网络-零基础入门.md)》。
> 那一篇里你手算过一个只会做二分类（sigmoid + 一个隐藏神经元）的最小网络。
> 这一篇把"下一步学什么"里的**第二步**补完：多分类怎么做、激活函数怎么选、优化器怎么选、怎么防止模型死记硬背。
> 同样全程手算加代码，看得懂就是真懂。

---

## 目录

1. [复习：二分类到多分类的鸿沟](#1-复习二分类到多分类的鸿沟)
2. [Softmax：把一组分数变成一组概率](#2-softmax把一组分数变成一组概率)
3. [交叉熵损失：衡量"分类错得多离谱"](#3-交叉熵损失衡量分类错得多离谱)
4. [手算一遍三分类](#4-手算一遍三分类)
5. [激活函数补完：ReLU 家族](#5-激活函数补完relu-家族)
6. [优化器进化史：SGD → Momentum → RMSProp → Adam](#6-优化器进化史sgd--momentum--rmsprop--adam)
7. [防止过拟合：正则化、Dropout、早停](#7-防止过拟合正则化dropout早停)
8. [完整代码：把四块基本功都用上](#8-完整代码把四块基本功都用上)
9. [黑话词典（续）](#9-黑话词典续)
10. [检查清单：到这里你应该会什么](#10-检查清单到这里你应该会什么)

---

## 1. 复习：二分类到多分类的鸿沟

上一篇讲的逻辑回归解决的是"是/否"问题：

```
概率 = σ(w·x + b)      输出 1 个数，落在 (0, 1)
```

但现实里大部分分类问题不止两类：

- 手写数字：0~9，**10 类**
- 图片里是猫/狗/鸟：**3 类**
- 下一个字该是哪个汉字：**几千类**

sigmoid 只会吐出一个数，没法直接告诉你"是猫的概率 0.7、是狗的概率 0.2、是鸟的概率 0.1"这种一组答案。

**需要的是一个函数：输入 N 个任意实数，输出 N 个概率，而且这 N 个概率加起来正好等于 1。**

这就是 Softmax。

---

## 2. Softmax：把一组分数变成一组概率

### 网络最后一层长什么样

多分类网络的输出层不再是 1 个神经元，而是**类别数个神经元**，每个神经元只做加权求和，先不激活，得到一组"分数"（术语叫 **logits**）：

```
z1, z2, ..., zK      ← K 个类别，K 个原始分数，可正可负，大小不设限
```

比如判断"猫/狗/鸟"，网络算出来：

```
z猫 = 2.0   z狗 = 1.0   z鸟 = 0.1
```

数字本身没有"概率"的含义，猫的分数高只能说明"网络觉得更像猫"，不能直接说"是猫的概率是 2.0"（概率不能大于 1）。

### Softmax 公式

```
softmax(z_i) = e^(z_i) / Σ_j e^(z_j)
```

翻译成人话：**每一项先取指数（保证非负），再除以所有项指数的总和（保证加起来是 1）**。

### 代入上面的例子

```
e^2.0 = 7.389
e^1.0 = 2.718
e^0.1 = 1.105

总和 = 7.389 + 2.718 + 1.105 = 11.212

p猫 = 7.389 / 11.212 = 0.659
p狗 = 2.718 / 11.212 = 0.242
p鸟 = 1.105 / 11.212 = 0.099

校验：0.659 + 0.242 + 0.099 = 1.000  ✓
```

网络现在给出一句人话："66% 觉得是猫，24% 觉得是狗，10% 觉得是鸟。"

### 为什么要先取指数，不直接除总和

直接用 `z_i / Σz_j` 不行，因为 `z` 可以是负数，比例会算出负的"概率"，没有意义。指数函数 `e^x` 永远大于 0，天然把"任意实数"映到"正数"，顺便还放大了差距——**分数最高的那一项会被指数进一步拉开优势**，这正是我们想要的（网络越自信，输出的概率应该越接近 1）。

### 一个记忆技巧

| 二分类 | 多分类 |
|---|---|
| Sigmoid | Softmax |
| 1 个输出，配合"1-p"表示另一类 | K 个输出，天然覆盖 K 类 |

**Sigmoid 其实是 Softmax 在 K=2 时的特例。** 这也是为什么很多资料把二者放在一起讲。

---

## 3. 交叉熵损失：衡量"分类错得多离谱"

### 为什么不用 MSE

上一篇用的均方误差 `(ŷ-y)²` 在分类问题上效果不好——概率是"够不够自信"的问题，不是"数值差多少"的问题。分类任务的标准答案是**交叉熵损失（Cross-Entropy Loss）**。

### 真实标签怎么表示：One-Hot

真实答案"这是一只猫"要编码成向量，用 **one-hot** 表示：类别对应位置是 1，其余是 0。

```
猫/狗/鸟三分类，真实答案是"猫"：
y = [1, 0, 0]
```

### 交叉熵公式

```
L = -Σ_i y_i × log(p_i)
```

因为 y 是 one-hot（只有真实类别那一位是 1，其余是 0），求和之后**只剩真实类别那一项**：

```
L = -log(p_真实类别)
```

**交叉熵损失只关心一件事：模型分配给正确答案的概率有多高。**

| 模型给真实类别的概率 p | 损失 -log(p) |
|---|---|
| 0.99（非常自信且答对） | 0.01（几乎没有损失） |
| 0.50（半信半疑） | 0.69 |
| 0.10（很不自信） | 2.30 |
| 0.01（几乎认为不可能，但恰好是对的） | 4.61（惩罚很重） |

`-log(p)` 这条曲线在 p 接近 0 时会冲向无穷大——**模型越是"自信地答错"，惩罚越狠**，这正是我们想要的：不但要答对，还要"该自信的时候自信"。

---

## 4. 手算一遍三分类

沿用第 2 节猫/狗/鸟的例子，真实答案是"猫"：

```
预测概率：p = [0.659, 0.242, 0.099]     (猫, 狗, 鸟)
真实标签：y = [1, 0, 0]

损失 L = -log(0.659) = 0.417
```

### 反向传播：Softmax + 交叉熵的梯度

这是整节最重要的结论，也是它俩总被绑在一起用的原因——**Softmax 配合交叉熵求导之后，梯度会化简成一个极其简单的式子**：

```
∂L/∂z_i = p_i - y_i
```

跟上一篇文章第 11 节 sigmoid 那一套"预测值减真实值"的结论**长得一模一样**，只是这次 p 和 y 都是向量。

代入数字：

```
∂L/∂z猫 = 0.659 - 1 = -0.341
∂L/∂z狗 = 0.242 - 0 =  0.242
∂L/∂z鸟 = 0.099 - 0 =  0.099
```

**读法**：z猫 的梯度是负的，说明"应该增大 z猫（让猫的分数更高）"；z狗、z鸟 的梯度是正的，说明"应该减小它们的分数"。这跟直觉完全一致——网络答对了大方向（猫的概率最高），但还不够自信，梯度在推它更自信。

后面这些梯度往回传给上一层权重的方式，跟上一篇第 11 节讲的链式法则完全一样，不再重复推导。

---

## 5. 激活函数补完：ReLU 家族

上一篇提到"ReLU 是现在的默认选择"，这里把它和它的变种讲透。

### ReLU 的问题：神经元死亡

```
ReLU(x) = max(0, x)
ReLU'(x) = 1  (x > 0)
         = 0  (x < 0)
```

问题在负半轴：一旦某个神经元的输入长期是负数，它的梯度永远是 0，**这个神经元就再也不会更新了**，相当于永久失活。这叫 **Dying ReLU**。

### 修补方案对照表

| 名字 | 公式 | 解决了什么 |
|---|---|---|
| **ReLU** | `max(0, x)` | 基准款，简单快，但会"死神经元" |
| **Leaky ReLU** | `max(0.01x, x)` | 负半轴给一个很小的斜率（如 0.01），不再彻底归零，神经元有机会复活 |
| **PReLU** | `max(αx, x)` | 负半轴的斜率 α 不再手动设，交给网络自己学 |
| **ELU** | `x (x>0)`；`α(e^x - 1) (x≤0)` | 负半轴是平滑曲线而不是折线，输出均值更接近 0，收敛更快 |
| **GELU** | `x × Φ(x)`（Φ 是正态分布累积函数） | 折线换成光滑曲线，Transformer / BERT / GPT 系列的默认选择 |

### 怎么选（经验规则，不是死规定）

```
不知道选什么     → 先用 ReLU，简单、快、90% 场景够用
训练中发现大量神经元“死了”（输出一直是 0） → 换 Leaky ReLU / ELU
在做 Transformer / 大模型                 → 用 GELU（已是社区默认）
输出层做二分类                            → Sigmoid（不是隐藏层，是最后一层）
输出层做多分类                            → Softmax（同上）
```

**记住一条边界**：隐藏层的激活函数（ReLU 家族）和输出层的激活函数（Sigmoid/Softmax）解决的是不同的问题，前者是"让网络能表达非线性"，后者是"把最后的分数转成概率"，不要混用。

---

## 6. 优化器进化史：SGD → Momentum → RMSProp → Adam

上一篇的更新公式：

```
新参数 = 旧参数 - 学习率 × 梯度
```

这就是**朴素 SGD**。它能用，但有两个明显缺点，于是后来的优化器一个个被发明出来修补它。

### 缺点一：容易在"峡谷"里来回震荡

想象损失函数的等高线是一个很扁的椭圆（一个方向很陡、另一个方向很平缓）。朴素 SGD 会在陡的方向上来回跳，在平缓的方向上又走得很慢——**很像下山时不断在山谷两壁间来回撞**。

### Momentum：给下降过程加惯性

解决办法很直觉：不能每一步都完全"忘记"上一步的方向，加一点"惯性"，让来回震荡的分量互相抵消，一致的方向被累加放大。

```
v = β × v_上一步 + 梯度
新参数 = 旧参数 - 学习率 × v
```

- `β` 通常取 0.9，可以理解成"保留 90% 的历史速度"
- 效果类似小球滚下山：震荡的方向被惯性抵消，一路向下的方向越滚越快

**数值感受**（1 维玩具例子，损失 `L(w) = (w-3)²`，起点 w=0，学习率 0.1，梯度 = 2(w-3)）：

| 步骤 | 朴素 SGD 的 w | Momentum 的 w（β=0.9） |
|---|---|---|
| 0 | 0.00 | 0.00 |
| 1 | 0.60 | 0.60 |
| 2 | 1.08 | 1.68 |
| 3 | 1.46 | 2.87 |
| 4 | 1.77 | 3.68（已经越过 3，开始往回修正） |

Momentum 前期冲得更快（借助累积的"速度"），到目标附近还会因为惯性稍微冲过头再修正回来，整体收敛通常比朴素 SGD 快。

### 缺点二：所有参数共用同一个学习率不合理

有的参数梯度一直很大（该走慢点），有的参数梯度一直很小（该走快点），但朴素 SGD 给所有参数用**同一个**学习率。

### RMSProp：给每个参数配一个自适应步长

思路：**记录每个参数梯度平方的滑动平均，梯度一直很大的参数就除以一个大数（步子变小），梯度一直很小的参数就除以一个小数（步子变大）**。

```
s = β × s_上一步 + (1-β) × 梯度²
新参数 = 旧参数 - 学习率 × 梯度 / (√s + ε)
```

- `ε`（比如 1e-8）纯粹是为了防止除以 0，不影响主要逻辑
- 这样每个参数都有了"专属"的有效学习率

### Adam：Momentum + RMSProp 二合一

**Adam（Adaptive Moment Estimation）** 把上面两个思路合并：既用 Momentum 的"速度"，又用 RMSProp 的"自适应步长"，外加一个专门修正训练初期偏差的技巧（bias correction，因为 v 和 s 刚开始是从 0 累积起来的，初期会偏小）。

```
m = β1 × m_上一步 + (1-β1) × 梯度          ← 一阶矩，类似 Momentum 的速度
s = β2 × s_上一步 + (1-β2) × 梯度²         ← 二阶矩，类似 RMSProp 的自适应尺度

m̂ = m / (1 - β1^t)      ← 偏差修正（t 是当前第几步）
ŝ = s / (1 - β2^t)

新参数 = 旧参数 - 学习率 × m̂ / (√ŝ + ε)
```

默认超参数（几乎不用改）：`β1 = 0.9`，`β2 = 0.999`，`ε = 1e-8`。

### 该用哪个

| 场景 | 建议 |
|---|---|
| 不知道用什么 | **Adam**，收敛快、对学习率不敏感、几乎是当前默认选择 |
| 追求极致的最终精度，愿意花时间调参 | SGD + Momentum，很多论文里精调过的 SGD 最终效果能反超 Adam |
| 教学 / 理解原理 | 从朴素 SGD 开始手推一遍，再上 Adam |

**一句话总结整条进化线**：SGD 只会沿着当前梯度走 → Momentum 给它加了"记忆"（不忘记之前的方向）→ RMSProp 给每个参数配了"自适应步子"→ Adam 把两者揉在一起，还顺手修正了初期的偏差。

---

## 7. 防止过拟合：正则化、Dropout、早停

### 先回忆过拟合是什么

上一篇黑话词典提过：**训练集准确率 99%，测试集只有 60%**，说明模型把训练数据"背"下来了，而不是学到了能泛化的规律。原因通常是**模型太复杂 / 参数太多 / 数据太少**。

### 方案一：L2 正则化（Weight Decay）

思路：**惩罚过大的权重**，逼着模型倾向于用更"平滑"、更简单的函数去拟合数据，而不是靠几个巨大的权重死记硬背个别样本。

```
L_new = L_原始 + λ × Σ(w²)
```

- `λ` 是正则化强度，越大惩罚越狠，模型越"保守"
- 加了这一项之后，求梯度时相当于每次更新都额外把权重往 0 拉一点，所以也叫 **weight decay（权重衰减）**：

```
新权重 = 旧权重 - 学习率 × (原始梯度 + 2λ × 旧权重)
       = 旧权重 × (1 - 2×学习率×λ) - 学习率 × 原始梯度
                 ↑
          每一步权重都被"打个折"再更新
```

### 方案二：Dropout

训练时，**每一步随机"关掉"一部分神经元**（让它们的输出临时变成 0），这一步用哪些神经元完全随机。

```
隐藏层原本：[a1, a2, a3, a4, a5]
Dropout(0.4) 之后随机变成：[a1, 0, a3, 0, a5]      ← 大约 40% 被关闭
```

**为什么这样能防过拟合**：网络没法把"记住答案"这件事全部依赖在某几个固定神经元身上——因为下一步它们可能就被关掉了。逼着每个神经元都要学到相对独立、有用的特征，等价于同时训练了大量"结构略有不同"的小网络，最后取平均效果。

**注意**：Dropout 只在训练时开启，**推理 / 预测阶段要关闭**，并把输出按保留比例缩放（现代框架的"inverted dropout"已经自动处理，你只需要记得 `model.eval()` 会自动关掉它）。

### 方案三：早停（Early Stopping）

同时观察训练集和验证集的损失曲线：

```
训练损失：一直下降（模型在越来越努力地拟合训练数据）
验证损失：先下降，到某一点后开始回升      ← 这个拐点就是过拟合开始的地方
```

**早停策略**：记录验证损失最低的那一轮模型，如果之后连续 N 轮（比如 10 轮）验证损失都没有刷新最低点，就停止训练，**用之前保存的"最优检查点"作为最终模型**，而不是最后一轮。

| 现象 | 说明 |
|---|---|
| 训练损失和验证损失一起下降 | 健康，继续训练 |
| 训练损失继续降，验证损失开始升 | 过拟合信号，该考虑早停了 |
| 两条都很高、都下不去 | 欠拟合，加大模型 / 训练更久，不是这里的问题 |

### 三个方案怎么组合用

它们不冲突，**实践中经常一起用**：L2 正则化让权重不要太大、Dropout 让网络不依赖单一神经元、早停防止训练过头。三管齐下，是训练一个"实际能用"的模型的标准操作。

---

## 8. 完整代码：把四块基本功都用上

在 `moons`（两个交织的月牙形）这个经典的二维小数据集上，训练一个**多层网络**，用上本篇讲的所有技巧：ReLU 隐藏层、Adam 优化器、L2 正则（weight_decay）、Dropout，输出层用 Softmax + 交叉熵（PyTorch 会把这两步自动合并进 `CrossEntropyLoss`）。

```python
import torch
import torch.nn as nn

# ============ 数据：随便造两类可分但不是线性可分的点 ============
torch.manual_seed(0)
n = 200
X = torch.randn(n, 2)
# 规则：距原点超过 1.2 记为类别 1，否则类别 0 —— 这是一个圆形边界，直线学不好
y = (X.norm(dim=1) > 1.2).long()

# 切分训练集 / 验证集
X_train, y_train = X[:160], y[:160]
X_val,   y_val   = X[160:], y[160:]

# ============ 网络结构：2 → 16(ReLU+Dropout) → 16(ReLU+Dropout) → 2(Softmax由损失函数内置) ============
model = nn.Sequential(
    nn.Linear(2, 16),
    nn.ReLU(),
    nn.Dropout(0.3),          # 训练时随机关闭 30% 神经元
    nn.Linear(16, 16),
    nn.ReLU(),
    nn.Dropout(0.3),
    nn.Linear(16, 2),         # 输出 2 个 logits，对应 2 个类别
)

criterion = nn.CrossEntropyLoss()   # 内部已经包含 Softmax，不需要你手动加
optimizer = torch.optim.Adam(model.parameters(), lr=0.01, weight_decay=1e-4)  # weight_decay 就是 L2 正则

best_val_loss = float("inf")
patience, patience_left = 15, 15     # 早停：验证损失连续 15 轮不刷新最优就停

for epoch in range(500):
    # ---------- 训练 ----------
    model.train()                    # 打开 Dropout
    optimizer.zero_grad()
    logits = model(X_train)          # 前向传播（未激活的原始分数）
    loss = criterion(logits, y_train)
    loss.backward()                  # 反向传播
    optimizer.step()                 # Adam 更新参数

    # ---------- 验证：关闭 Dropout，看真实泛化表现 ----------
    model.eval()
    with torch.no_grad():
        val_logits = model(X_val)
        val_loss = criterion(val_logits, y_val).item()

    # ---------- 早停判断 ----------
    if val_loss < best_val_loss:
        best_val_loss = val_loss
        patience_left = patience
        best_state = {k: v.clone() for k, v in model.state_dict().items()}
    else:
        patience_left -= 1
        if patience_left == 0:
            print(f"验证损失连续 {patience} 轮没有刷新，第 {epoch} 轮提前停止")
            break

    if epoch % 50 == 0:
        print(f"epoch {epoch:4d}  train_loss={loss.item():.4f}  val_loss={val_loss:.4f}")

# 恢复验证集表现最好的那一版参数，而不是最后一轮
model.load_state_dict(best_state)

# ============ 看结果：用 softmax 把 logits 转成概率 ============
model.eval()
with torch.no_grad():
    probs = torch.softmax(model(X_val), dim=1)
    preds = probs.argmax(dim=1)
    acc = (preds == y_val).float().mean()
    print(f"\n验证集准确率：{acc.item():.2%}")
```

**逐句对照本篇知识点**：

| 代码 | 对应知识点 |
|---|---|
| `nn.ReLU()` | 第 5 节的激活函数 |
| `nn.Dropout(0.3)` | 第 7 节的 Dropout |
| `nn.CrossEntropyLoss()` | 第 2、3 节的 Softmax + 交叉熵（PyTorch 把两者合并成一步，更稳定） |
| `torch.optim.Adam(..., weight_decay=1e-4)` | 第 6 节的 Adam 优化器 + 第 7 节的 L2 正则 |
| `patience` 相关的那一段 | 第 7 节的早停 |
| `model.train()` / `model.eval()` | 训练时开 Dropout，验证 / 推理时关 Dropout |

---

## 9. 黑话词典（续）

| 术语 | 人话解释 |
|---|---|
| **Logits** | 网络最后一层、还没经过 Softmax/Sigmoid 的原始分数，可正可负 |
| **Softmax** | 把一组 logits 转成一组加起来等于 1 的概率 |
| **One-Hot** | 用"只有一位是 1，其余全是 0"的向量表示类别标签 |
| **交叉熵 Cross-Entropy** | 分类任务的标准损失函数，只关心"真实类别被分到多少概率" |
| **Dying ReLU** | ReLU 负半轴梯度恒为 0，神经元永久停止更新的现象 |
| **Momentum** | 优化器给参数更新加上"惯性"，抑制震荡、加速收敛 |
| **RMSProp** | 给每个参数配一个随梯度大小自适应调整的学习率 |
| **Adam** | Momentum + RMSProp 的合体，外加偏差修正，目前最常用的默认优化器 |
| **权重衰减 Weight Decay** | 等价于 L2 正则化，每一步都把权重朝 0 轻轻拉一点 |
| **Dropout** | 训练时随机关闭一部分神经元，防止网络依赖个别神经元 |
| **早停 Early Stopping** | 验证损失不再刷新最优时提前结束训练，取之前最优的那个检查点 |
| **训练 / 验证 / 推理模式** | `model.train()` 打开 Dropout 等训练专属行为，`model.eval()` 关闭它们 |

---

## 10. 检查清单：到这里你应该会什么

- [ ] 能说清楚 Softmax 和 Sigmoid 的关系（K=2 的特例）
- [ ] 能手算一个 3 类问题的 Softmax 概率和交叉熵损失
- [ ] 知道 `∂L/∂z = p - y` 这个结论，以及它为什么和 sigmoid 的梯度长得一样
- [ ] 能说出至少 2 种 ReLU 的变种，以及各自解决了什么问题
- [ ] 能画出 SGD → Momentum → RMSProp → Adam 的演进逻辑，而不是死记公式
- [ ] 知道 L2 正则、Dropout、早停各自的原理，以及为什么可以一起用
- [ ] 能读懂第 8 节的完整代码，并指出每一行对应哪个知识点

全部打勾之后，再回到入门笔记第 15 节的**第三步**：挑一个方向（CNN / RNN / Transformer）深入。

---

*和上一篇一样，所有数字都可以用计算器或几行 Python 复现，对不上就回去重新算一遍。*
