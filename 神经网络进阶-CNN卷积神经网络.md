# 神经网络进阶：CNN 卷积神经网络（图像方向）

> 承接《[从机器学习到神经网络：零基础入门](从机器学习到神经网络-零基础入门.md)》第 15 节"第三步"。
> 这一篇挑的第一个方向是 **CNN（卷积神经网络）**，专门处理图像。
> 核心问题只有一个：**为什么不能直接把图片像素塞进第一篇讲的全连接网络？**

---

## 目录

1. [全连接网络处理图像的两个硬伤](#1-全连接网络处理图像的两个硬伤)
2. [卷积：一个滑动的小窗口](#2-卷积一个滑动的小窗口)
3. [手算一遍卷积](#3-手算一遍卷积)
4. [Padding 与 Stride](#4-padding-与-stride)
5. [池化：给特征图"降采样"](#5-池化给特征图降采样)
6. [参数量对比：卷积为什么省参数](#6-参数量对比卷积为什么省参数)
7. [感受野：层数越深，看得越"广"](#7-感受野层数越深看得越广)
8. [经典架构脉络](#8-经典架构脉络)
9. [完整代码：一个能训练的小 CNN](#9-完整代码一个能训练的小-cnn)
10. [黑话词典](#10-黑话词典)
11. [检查清单](#11-检查清单)

---

## 1. 全连接网络处理图像的两个硬伤

一张很小的图片，比如 100×100 像素、RGB 三通道，展平成一维向量：

```
100 × 100 × 3 = 30,000 个数
```

如果第一个隐藏层只有 256 个神经元，第一层权重就有：

```
30,000 × 256 = 7,680,000 个参数    ← 只是第一层
```

**硬伤一：参数量爆炸。** 图片稍微大一点（现实中常见的 224×224），参数量会再翻十几倍，训练又慢又容易过拟合。

**硬伤二：展平丢失了空间结构。** 图片里"相邻的像素往往相关"（一只眼睛的像素挨在一起，边缘的像素连成线），但展平成一维向量后，网络必须重新"发现"哪些位置在图像里是邻居——这是在浪费网络的学习能力去学一个我们本来就知道的先验知识。

**CNN 的核心思想**：不要展平，直接在二维图像上滑动一个很小的窗口去找局部规律，而且**这个小窗口的参数在整张图片上是共享的**。

---

## 2. 卷积：一个滑动的小窗口

### 卷积核（Filter / Kernel）

一个卷积核就是一个很小的数字矩阵，比如 3×3：

```
卷积核 K =
  [ 1   0  -1 ]
  [ 1   0  -1 ]
  [ 1   0  -1 ]
```

这个具体的核其实是经典的"竖直边缘检测器"——左边是正数、右边是负数，遇到"左边亮、右边暗"的竖直边缘时输出会特别大。

### 卷积怎么算

把卷积核盖在图像的一小块区域上，**对应位置相乘再全部加起来**，得到输出特征图（feature map）里的一个数；然后卷积核向右滑动一格，重复上面的计算；滑到头了就换下一行——像"拿一个小放大镜扫过整张图"。

```
输出特征图上的一个值 = Σ (核里的每个数 × 图像对应位置的像素值)
```

**关键**：这一个卷积核在扫过整张图片的过程中，**自己的 9 个数字（3×3）从头到尾不变**。这就是"权重共享"——学到的"竖直边缘检测器"在图片任何位置出现竖直边缘都能被识别，而不需要给图片的每个位置单独学一套参数。

---

## 3. 手算一遍卷积

拿一个 4×4 的灰度图像和一个 3×3 的卷积核，手算左上角第一个输出值。

**输入图像（4×4）：**

```
[ 1  2  0  1 ]
[ 0  1  3  1 ]
[ 2  1  0  0 ]
[ 1  0  1  2 ]
```

**卷积核（3×3）：**

```
[ 1  0 -1 ]
[ 1  0 -1 ]
[ 1  0 -1 ]
```

卷积核先盖在图像左上角的 3×3 区域：

```
[ 1  2  0 ]
[ 0  1  3 ]
[ 2  1  0 ]
```

对应位置相乘再求和：

```
(1×1)+(2×0)+(0×-1)
+ (0×1)+(1×0)+(3×-1)
+ (2×1)+(1×0)+(0×-1)

= (1 + 0 + 0) + (0 + 0 - 3) + (2 + 0 + 0)
= 1 - 3 + 2
= 0
```

**输出特征图左上角第一个值是 0。** 把卷积核向右滑动一格，盖住图像的第 2~4 列，重复同样的乘加运算，得到第二个输出值；滑到最右边后，整体下移一行，从头再来。4×4 的图配 3×3 的核（不加 padding），一共能滑出 **2×2** 个输出位置。

---

## 4. Padding 与 Stride

### Padding：图像边缘要不要"垫一圈 0"

上面的例子能看到，卷积之后图像从 4×4 缩小到了 2×2——**每卷一层，图就变小一点**，层数一多，图会缩没。解决办法是在图像四周垫一圈（或几圈）0，叫 **padding**：

```
不加 padding（valid）：  输出尺寸 = 输入尺寸 - 核尺寸 + 1     （会变小）
加 padding 使输出不变（same）：  在四周垫的 0 的圈数 = (核尺寸-1)/2
```

### Stride：滑动的步长

上面的例子每次滑动 1 格，这叫 **stride=1**。如果 stride=2，核每次跳 2 格，输出特征图会更小、计算更快，代价是可能漏掉一些细节。

```
输出尺寸 ≈ (输入尺寸 + 2×padding - 核尺寸) / stride + 1
```

---

## 5. 池化：给特征图"降采样"

池化（Pooling）不带参数，只是简单地压缩特征图尺寸，最常见的是 **Max Pooling**：把特征图切成不重叠的小块（比如 2×2），每块只留下最大值。

```
输入 2×2 小块：
[ 3  1 ]
[ 0  2 ]

Max Pooling 输出：3      ← 只保留这一块里"反应最强烈"的那个值
```

**为什么这样做有用**：
- **减少计算量**：特征图尺寸直接减半
- **增加平移不变性**：物体在图片里稍微挪动一点位置，最大值所在的小块可能不变，输出基本不变——这正是我们希望的"猫挪到画面左边一点，网络还是要认出这是猫"

---

## 6. 参数量对比：卷积为什么省参数

沿用第 1 节 100×100×3 的图像，改用一个 3×3 的卷积核（3 通道输入，1 个输出通道）：

```
卷积核参数 = 3×3×3 + 1(偏置) = 28 个参数
```

**对比**：

| 方式 | 参数量 | 差距 |
|---|---|---|
| 全连接第一层（256 个神经元） | 7,680,000 | —— |
| 一个 3×3 卷积核 | 28 | 少了 27 万倍 |

现实中一层会用多个卷积核（比如 64 个），参数量是 `28 × 64 ≈ 1,792`，**依然比全连接少几千倍**。这就是为什么处理图像必须用卷积，而不是硬套全连接。

---

## 7. 感受野：层数越深，看得越"广"

单独一个 3×3 卷积，每个输出位置只能"看到"输入图像里 3×3 的一小块，这块区域叫 **感受野（Receptive Field）**。

但把卷积层堆叠起来，感受野会像滚雪球一样变大：

```
第 1 层 3×3 卷积：感受野 3×3
第 2 层再叠一个 3×3 卷积：感受野变成 5×5     ← 因为第2层的每个输入本身已经综合了3×3的信息
第 3 层再叠一个：感受野变成 7×7
```

**规律**：每多叠一层 3×3 卷积，感受野每边增加 2。**堆叠很多层小卷积，最终能让网络的"眼睛"看到整张图**，同时参数量比一上来就用一个大卷积核（比如 15×15）少得多——这也是现代 CNN 普遍偏爱"很多层 3×3"而不是"少数几层大卷积"的原因。

---

## 8. 经典架构脉络

| 架构 | 年份 | 关键贡献 |
|---|---|---|
| **LeNet** | 1998 | 最早的卷积+池化组合，识别手写数字，CNN 的雏形 |
| **AlexNet** | 2012 | 用 ReLU（回忆上一篇第 5 节）+ Dropout（第二篇第 7 节）在 ImageNet 上大幅超越传统方法，深度学习"复兴"的起点 |
| **VGG** | 2014 | 证明"堆叠很多层 3×3 小卷积"比少数几层大卷积更好（呼应第 7 节的感受野结论） |
| **ResNet** | 2015 | 引入**残差连接（skip connection）**：`输出 = 卷积(x) + x`，让梯度能跳过中间层直接回传，解决了上一篇第 11 节讲过的深层网络梯度消失问题，第一次把网络做到上百层 |

**看到没有**：ResNet 的残差连接，正是入门笔记里"梯度消失的两个解法"之一（另一个是 ReLU）——**前面几篇讲的基本功，在这里全部串联了起来**。

---

## 9. 完整代码：一个能训练的小 CNN

在 MNIST 风格的 28×28 灰度手写数字上训练一个小型 CNN，用上卷积、池化、ReLU、Dropout：

```python
import torch
import torch.nn as nn

class SmallCNN(nn.Module):
    def __init__(self, num_classes=10):
        super().__init__()
        self.features = nn.Sequential(
            nn.Conv2d(1, 16, kernel_size=3, padding=1),   # 28x28 -> 28x28，16 个卷积核
            nn.ReLU(),
            nn.MaxPool2d(2),                              # 28x28 -> 14x14

            nn.Conv2d(16, 32, kernel_size=3, padding=1),  # 14x14 -> 14x14，32 个卷积核
            nn.ReLU(),
            nn.MaxPool2d(2),                              # 14x14 -> 7x7
        )
        self.classifier = nn.Sequential(
            nn.Flatten(),                     # 到这一步才展平，交给全连接层做最终判断
            nn.Linear(32 * 7 * 7, 64),
            nn.ReLU(),
            nn.Dropout(0.3),
            nn.Linear(64, num_classes),        # 输出 10 个 logits，配合上一篇的 CrossEntropyLoss
        )

    def forward(self, x):
        x = self.features(x)      # 卷积 + 池化：提取局部特征
        x = self.classifier(x)    # 全连接：综合特征做分类
        return x

model = SmallCNN()
criterion = nn.CrossEntropyLoss()
optimizer = torch.optim.Adam(model.parameters(), lr=1e-3, weight_decay=1e-4)

# 假数据演示一次前向传播：batch=8 张 28x28 灰度图
x = torch.randn(8, 1, 28, 28)
y = torch.randint(0, 10, (8,))

logits = model(x)
loss = criterion(logits, y)
loss.backward()
optimizer.step()
print("输出形状:", logits.shape, " 损失:", loss.item())
```

**逐层对照本篇知识点**：`Conv2d` 是第 2、3 节的卷积核滑动、`MaxPool2d` 是第 5 节的池化、两层卷积堆叠对应第 7 节的感受野扩大、最后展平接全连接对应"卷积负责找局部特征，全连接负责综合判断"。

---

## 10. 黑话词典

| 术语 | 人话解释 |
|---|---|
| **卷积核 / Filter / Kernel** | 一个很小的、在整张图上共享的可学习参数矩阵 |
| **特征图 Feature Map** | 卷积输出的结果，每个位置反映"这里有多像卷积核要找的模式" |
| **通道 Channel** | 一张图可以有多个"层"（RGB 是 3 通道），卷积核的深度要匹配输入通道数 |
| **Padding** | 在图像边缘垫 0，控制卷积后尺寸是否缩小 |
| **Stride** | 卷积核每次滑动的步长 |
| **池化 Pooling** | 不带参数的下采样操作，最常见 Max Pooling |
| **感受野 Receptive Field** | 输出特征图上一个点，实际能"看到"输入图像里多大的一块区域 |
| **残差连接 Skip Connection** | 让输出等于"卷积结果 + 输入本身"，帮梯度绕过中间层直接回传 |

---

## 11. 检查清单

- [ ] 能说出全连接网络处理图像的两个硬伤
- [ ] 能手算一次 3×3 卷积核在 4×4 图像上的一个输出值
- [ ] 知道 padding 和 stride 分别控制什么
- [ ] 能解释 Max Pooling 为什么能带来一定的平移不变性
- [ ] 能算出一个具体例子里"全连接 vs 卷积"的参数量差距
- [ ] 知道感受野怎么随层数增长，以及 VGG 的洞察是什么
- [ ] 能说出 ResNet 解决了什么问题，以及它和"梯度消失"的关系

打勾之后，可以去玩一下 MNIST 手写数字识别的完整项目（呼应入门笔记第 15 节的"第四步"）。

---

*本文的卷积手算例子可以直接在纸上或 Excel 里重新验证一遍。*
