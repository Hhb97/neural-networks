# 从机器学习到神经网络：零基础入门

> 读完这份文档，你会明白：机器到底"学"的是什么、它怎么学、以及神经网络凭什么比传统方法强。
> 全文只用到加减乘除和一点点求导，所有例子都可以拿计算器手算验证。

---

## 目录

1. [机器学习到底是什么](#1-机器学习到底是什么)
2. [第一个模型：预测房价](#2-第一个模型预测房价)
3. [机器怎么"学"：损失函数](#3-机器怎么学损失函数)
4. [下山：梯度下降](#4-下山梯度下降)
5. [手算一遍完整训练](#5-手算一遍完整训练)
6. [从预测数值到判断类别](#6-从预测数值到判断类别)
7. [直线的极限：为什么需要神经网络](#7-直线的极限为什么需要神经网络)
8. [一个神经元长什么样](#8-一个神经元长什么样)
9. [把神经元连成网络](#9-把神经元连成网络)
10. [前向传播：数据怎么流过去](#10-前向传播数据怎么流过去)
11. [反向传播：错误怎么传回来](#11-反向传播错误怎么传回来)
12. [完整训练循环](#12-完整训练循环)
13. [可运行代码](#13-可运行代码)
14. [黑话词典](#14-黑话词典)
15. [下一步学什么](#15-下一步学什么)

---

## 1. 机器学习到底是什么

### 和传统编程的区别

传统编程，是你把规则写死：

```java
// 判断是不是垃圾邮件 —— 传统写法
if (title.contains("中奖") || title.contains("免费领取")) {
    return SPAM;
}
```

问题很明显：骗子换个词写成"中·奖"，你的规则就废了。你得不停加 if。

机器学习反过来：**你不写规则，你给例子，让程序自己总结规则。**

```
传统编程：  规则 + 数据  →  答案
机器学习：  数据 + 答案  →  规则
```

你给它 10 万封邮件，每封都标好"是垃圾/不是垃圾"，程序自己找出规律。这个"找出来的规律"就叫**模型（model）**。

### 一句话定义

> 机器学习 = 用数据自动找到一个函数，这个函数能把输入映射到正确的输出。

### 三种学习方式

| 类型 | 有没有标准答案 | 例子 |
|---|---|---|
| **监督学习** | 有。每条数据都带答案 | 房价预测、垃圾邮件识别、图像分类 |
| **无监督学习** | 没有。自己找结构 | 用户分群、异常检测、推荐 |
| **强化学习** | 没有答案，但有奖惩 | 下棋、自动驾驶、游戏 AI |

**本文全程只讲监督学习**，它是最常用也最容易理解的一种，神经网络的基础训练方式也是它。

---

## 2. 第一个模型：预测房价

先从最简单的问题开始。假设你收集了一些数据：

| 面积（㎡） | 价格（万） |
|---|---|
| 50 | 150 |
| 60 | 180 |
| 70 | 210 |

肉眼就能看出规律：**价格 = 面积 × 3**。

写成公式：

```
预测价格 = w × 面积 + b
```

- `w` 叫**权重（weight）**，控制"每平米多少钱"，这里 w = 3
- `b` 叫**偏置（bias）**，是个基础值，相当于直线的截距，这里 b = 0

`w` 和 `b` 就是这个模型的**参数**。所谓"训练模型"，就是**找到最合适的 w 和 b**。

### 换个说法

初中数学里的一次函数 `y = kx + b`，机器学习里换了个名字叫**线性回归**。就这么简单。区别只在于：

- 初中：老师给你 k 和 b，你算 y
- 机器学习：给你一堆 (x, y)，你反过来求 k 和 b

### 但现实没这么理想

真实数据不会这么整齐：

| 面积 | 真实价格 | 如果 w=3 的预测 | 差多少 |
|---|---|---|---|
| 50 | 155 | 150 | -5 |
| 60 | 175 | 180 | +5 |
| 70 | 215 | 210 | -5 |

没有任何一条直线能穿过所有点。所以目标变成：**找一条"总体上最接近"的直线**。

"最接近"需要一个量化标准，这就引出了下一节。

---

## 3. 机器怎么"学"：损失函数

### 怎么衡量"错得多离谱"

第一反应：把误差加起来。但正负会抵消 —— 上面那三个误差 `-5 + 5 - 5 = -5`，看起来只错了 5，实际每条都错了 5。

解决办法：**平方**。平方后全是正数，而且错得越远惩罚越重（差 2 倍，惩罚 4 倍）。

这就是**均方误差（MSE, Mean Squared Error）**：

```
损失 L = (1/n) × Σ (预测值 - 真实值)²
```

算一下上面的例子：

```
L = [(-5)² + 5² + (-5)²] / 3 = (25 + 25 + 25) / 3 = 25
```

### 损失函数是什么

**损失函数（Loss Function）就是"错误程度的计分板"。**

- 损失大 = 模型很烂
- 损失小 = 模型不错
- 损失为 0 = 完美拟合（现实中基本不可能，而且往往是过拟合）

> **训练的全部目标，就是让损失变小。** 后面所有的数学，都是在服务这一件事。

### 一个小习惯

你在教程里会经常看到损失前面多个 1/2：

```
L = (1/2n) × Σ (预测值 - 真实值)²
```

这个 1/2 **不影响最优解在哪**（乘个正常数不改变最低点位置），纯粹是因为平方求导时会掉下来一个 2，正好约掉，让后面的公式干净。看到它别慌，就是个化简技巧。

---

## 4. 下山：梯度下降

### 问题

我们要找让损失 L 最小的 `w` 和 `b`。怎么找？

**笨办法**：把 w 从 -100 试到 100，每个都算一遍损失，挑最小的。参数只有一两个还行，神经网络有几亿个参数，这条路直接堵死。

**聪明办法**：梯度下降。

### 下山比喻

想象你在浓雾里的山上，要走到山谷最低处。你看不见全局，但你能感受到**脚下哪个方向是下坡**。

策略很自然：

1. 感受当前位置哪边最陡峭地向下
2. 朝那个方向迈一小步
3. 重复，直到脚下变平（走到谷底）

**梯度下降就是这个策略的数学版**：

| 比喻 | 数学 |
|---|---|
| 山的高度 | 损失 L |
| 你的位置 | 当前参数 w、b |
| 脚下的坡度 | **梯度**（导数） |
| 步子大小 | **学习率** η |
| 谷底 | 损失最小的参数 |

### 梯度是什么

**梯度就是导数**，含义是：**参数动一点点，损失会动多少。**

- 梯度是正的 → 参数增大会让损失变大 → 应该减小参数
- 梯度是负的 → 参数增大会让损失变小 → 应该增大参数

不管哪种情况，都是**朝梯度的反方向走**。所以更新公式带一个负号：

```
新参数 = 旧参数 - 学习率 × 梯度
```

### 学习率：步子迈多大

学习率 `η`（读作 eta）是你唯一必须手动设的关键值。

| 学习率 | 会发生什么 |
|---|---|
| 太小（0.0001） | 走得太慢，训练几天还没到谷底 |
| 合适（0.01 ~ 0.1） | 平稳下降 ✓ |
| 太大（1.0+） | 一步跨过谷底，越跳越远，损失变成 NaN |

**怎么判断**：看损失曲线。平滑下降就对了；来回锯齿说明太大；几乎是水平线说明太小。

### 房价例子的梯度公式

对 `L = (1/2n) Σ (w·x + b - y)²` 求导（推导过程见附录思路，先接受结论）：

```
误差 e = 预测值 - 真实值 = (w·x + b) - y

∂L/∂w = 平均值( e × x )      ← 对 w 的梯度
∂L/∂b = 平均值( e )          ← 对 b 的梯度
```

记住这两行，下一节我们拿它手算。

---

## 5. 手算一遍完整训练

**数据**（故意用最简单的数字）：

| x | 1 | 2 | 3 |
|---|---|---|---|
| y | 3 | 5 | 7 |

肉眼可见答案是 `y = 2x + 1`，我们看机器能不能自己找到。

**起点**：`w = 0, b = 0`（一无所知），**学习率 η = 0.1**

### 第 1 轮

```
预测：ŷ = 0×x + 0 = [0, 0, 0]
误差：e = ŷ - y = [0-3, 0-5, 0-7] = [-3, -5, -7]

∂L/∂w = ( (-3)×1 + (-5)×2 + (-7)×3 ) / 3
      = ( -3 - 10 - 21 ) / 3 = -34/3 = -11.333

∂L/∂b = ( -3 - 5 - 7 ) / 3 = -5

更新：
w = 0 - 0.1 × (-11.333) = 1.133      ← 梯度是负的，所以 w 增大
b = 0 - 0.1 × (-5)      = 0.500
```

### 第 2 轮

```
预测：ŷ = 1.133×[1,2,3] + 0.5 = [1.633, 2.767, 3.900]
误差：e = [-1.367, -2.233, -3.100]      ← 比上一轮小多了

∂L/∂w = ( -1.367×1 + -2.233×2 + -3.100×3 ) / 3 = -5.044
∂L/∂b = ( -1.367 - 2.233 - 3.100 ) / 3 = -2.233

w = 1.133 + 0.504 = 1.638
b = 0.500 + 0.223 = 0.723
```

### 第 3 轮

```
预测：ŷ = [2.361, 3.999, 5.637]
误差：e = [-0.639, -1.001, -1.363]

∂L/∂w = -2.244  →  w = 1.638 + 0.224 = 1.862
∂L/∂b = -1.001  →  b = 0.723 + 0.100 = 0.823
```

### 汇总

| 轮次 | w | b | 损失 |
|---|---|---|---|
| 0（起点） | 0.000 | 0.000 | 13.833 |
| 1 | 1.133 | 0.500 | 2.744 |
| 2 | 1.638 | 0.723 | 0.545 |
| 3 | 1.862 | 0.823 | 0.109 |
| ... | ↓ | ↓ | ↓ |
| 收敛 | **2.000** | **1.000** | **0.000** |

**这就是机器学习的全部核心流程。** 后面所有复杂的模型，包括 GPT，训练时干的都是同一件事，只是参数从 2 个变成了几千亿个。

> **阶段小结（这里必须懂）**
> 1. 定一个带参数的函数（模型）
> 2. 定一个衡量错误的标准（损失函数）
> 3. 算出"参数怎么改能让错误变小"（梯度）
> 4. 改一小步，重复几万次（梯度下降）

---

## 6. 从预测数值到判断类别

刚才预测的是价格（连续数值），叫**回归**。但很多问题是判断"是/否"，叫**分类**：

- 这封邮件是垃圾邮件吗？
- 这张图是猫吗？
- 这个用户会流失吗？

### 问题在哪

直线 `w·x + b` 的输出范围是负无穷到正无穷。但分类需要输出一个**概率**，必须落在 0 到 1 之间。

### Sigmoid：一个压缩器

```
σ(t) = 1 / (1 + e^(-t))
```

它的作用是把任意实数压到 (0, 1) 区间：

| 输入 t | -5 | -2 | -1 | 0 | 1 | 2 | 5 |
|---|---|---|---|---|---|---|---|
| σ(t) | 0.007 | 0.12 | 0.27 | **0.5** | 0.73 | 0.88 | 0.993 |

形状是一条平滑的 S 曲线，两头趋近 0 和 1，中间在 t=0 处过 0.5。

于是分类模型变成：

```
概率 = σ(w·x + b)

概率 > 0.5  →  判为"是"
概率 ≤ 0.5  →  判为"否"
```

这个模型叫**逻辑回归**。虽然名字带"回归"，但它做的是分类。

**关键**：训练方式和线性回归**完全一样** —— 定损失、求梯度、往下走。只是中间多了一个 σ 挤压的步骤。

---

## 7. 直线的极限：为什么需要神经网络

### 一个学不会的问题

看这四个点，任务是把 ● 和 ○ 分开：

```
     x2
      ↑
   1  |  ●        ○
      |
   0  |  ○        ●
      +---------------→ x1
         0        1
```

| x1 | x2 | 类别 |
|---|---|---|
| 0 | 0 | ○ |
| 0 | 1 | ● |
| 1 | 0 | ● |
| 1 | 1 | ○ |

这是**异或（XOR）**：两个输入不同时为 ●，相同时为 ○。

**拿一支笔试试，你画不出任何一条直线能把 ● 和 ○ 分到两边。** 不管怎么画，总有一个点站错队。

这不是算力不够，是**数学上不可能** —— 线性模型只能画直线（高维叫超平面），而这个问题本质上需要一条弯的边界。

1969 年，这个问题几乎让整个神经网络研究停滞了十几年。

### 破局思路

一条直线不行，那**用两条直线，再把它们的结果组合一下**呢？

- 第一条直线判断"x1 + x2 > 0.5 吗"
- 第二条直线判断"x1 + x2 > 1.5 吗"
- 第三步：把这两个判断结果再组合一次

**这就是神经网络**：把多个简单的线性判断分层堆叠起来，组合出复杂的决策边界。

---

## 8. 一个神经元长什么样

一个神经元只做两件事：

```
    x1 ──w1──┐
             ├──→ [ 加权求和 ] ──→ [ 激活函数 ] ──→ 输出
    x2 ──w2──┘         z              a = σ(z)
             ↑
            +b
```

**第一步：加权求和（线性）**

```
z = w1·x1 + w2·x2 + ... + b
```

就是第 2 节那个 `w·x + b`，只是输入多了几个。

**第二步：激活（非线性）**

```
a = σ(z)
```

用 sigmoid 或别的激活函数压一下。

### 为什么第二步不能省

**这是整份文档最重要的一句话。**

假如没有激活函数，两层叠起来是：

```
第一层：z1 = w1·x + b1
第二层：z2 = w2·z1 + b2
       = w2·(w1·x + b1) + b2
       = (w2·w1)·x + (w2·b1 + b2)
       = W·x + B          ← 又变回一条直线了！
```

**线性套线性还是线性。** 没有激活函数，你堆 1000 层也等价于一条直线，照样解不了异或。

> **激活函数是神经网络能拟合任意复杂曲线的唯一原因。**

### 常见激活函数

| 名字 | 公式 | 特点 |
|---|---|---|
| **Sigmoid** | `1/(1+e^-x)` | 输出 (0,1)，适合表示概率。缺点：深层会梯度消失 |
| **Tanh** | `(e^x-e^-x)/(e^x+e^-x)` | 输出 (-1,1)，比 sigmoid 好一点 |
| **ReLU** | `max(0, x)` | **现在的默认选择**。极简、极快、不容易梯度消失 |
| **Leaky ReLU** | `max(0.01x, x)` | 修补 ReLU 负半轴永久失活的问题 |

ReLU 简单到让人怀疑：负数变 0，正数不变。就这。但它是深度学习能训练到几百层的关键功臣之一。

---

## 9. 把神经元连成网络

把神经元排成列，一列叫一**层（layer）**：

```
  输入层          隐藏层           输出层
   (x)          (a1, a2)           (ŷ)

   x1 ──────→   ( ○ )  ──────→
                                    ( ○ ) ──→ 预测
   x2 ──────→   ( ○ )  ──────→
```

| 层 | 作用 |
|---|---|
| **输入层** | 不做计算，只是数据的入口。有几个特征就有几个节点 |
| **隐藏层** | 真正干活的地方。层数和节点数由你决定 |
| **输出层** | 给出最终结果。分类任务几个类别就几个节点 |

**"深度学习"的深，就是指隐藏层多。** 一层叫浅层网络，几十上百层叫深度网络。

### 名词对照

| 说法 | 意思 |
|---|---|
| 全连接层 / Dense / Linear | 上一层每个节点都连到下一层每个节点 |
| 网络的"宽度" | 每层有多少个神经元 |
| 网络的"深度" | 有多少层 |
| 网络的"参数量" | 所有 w 和 b 加起来的总数 |

一个"输入 2 → 隐藏 3 → 输出 1"的网络，参数量是：

```
第一层：2×3 个 w + 3 个 b = 9
第二层：3×1 个 w + 1 个 b = 4
总计 13 个参数
```

GPT-3 是 1750 亿个。原理完全一样，只是数量级不同。

---

## 10. 前向传播：数据怎么流过去

**前向传播（Forward Propagation）= 从输入算到输出。**

用一个最小的网络（1 输入 → 1 隐藏 → 1 输出）走一遍，数字全给全。

### 设定

```
输入      x  = 1.0
真实答案  y  = 1
参数      w1 = 0.5,  b1 = 0
          w2 = 0.8,  b2 = 0
```

### 计算流水线

```
x ──→ z1 = w1·x + b1 ──→ a1 = σ(z1) ──→ z2 = w2·a1 + b2 ──→ ŷ = σ(z2) ──→ L
```

这一行箭头图翻译成代码就是：

```java
double z1   = w1 * x + b1;                   // 第一层：加权求和
double a1   = sigmoid(z1);                   // 第一层：激活
double z2   = w2 * a1 + b2;                  // 第二层：加权求和
double yHat = sigmoid(z2);                   // 第二层：激活 → 预测值
double loss = 0.5 * Math.pow(yHat - y, 2);   // 和答案比，算损失
```

### 代入数字

| 步骤 | 算式 | 结果 |
|---|---|---|
| z1 | 0.5 × 1.0 + 0 | **0.500** |
| a1 | σ(0.5) = 1/(1+e^-0.5) | **0.6225** |
| z2 | 0.8 × 0.6225 + 0 | **0.4980** |
| ŷ | σ(0.4980) | **0.6220** |
| L | 0.5 × (0.6220 − 1)² | **0.0714** |

真实答案是 1，模型猜了 0.622，损失 0.0714。**现在要让这个数字变小。**

### 符号速查

| 符号 | 读法 | 含义 |
|---|---|---|
| `x` | x | 输入 |
| `w` | w | 权重，要训练的参数 |
| `b` | b | 偏置，要训练的参数 |
| `z` | z | 加权和，还没激活的中间值 |
| `a` | a | 激活后的值 |
| `ŷ` | y-hat | 模型的预测值 |
| `y` | y | 真实答案（数据自带的，不是算的） |
| `L` | Loss | 损失 |
| 下标 1、2 | | 第几层，不是次方 |

---

## 11. 反向传播：错误怎么传回来

现在知道错了 0.0714，但**该怪谁？w1 还是 w2？各改多少？**

回答这个问题的方法就是**反向传播（Backpropagation）**。

### 核心工具：链式法则

链式法则的直觉，用齿轮想最清楚：

> 大齿轮转 1 圈，中齿轮转 2 圈；中齿轮转 1 圈，小齿轮转 3 圈。
> 那么大齿轮转 1 圈，小齿轮转 **2 × 3 = 6** 圈。

**影响是一路乘过去的。**

`w1` 影响 `z1`，`z1` 影响 `a1`，`a1` 影响 `z2`，`z2` 影响 `ŷ`，`ŷ` 影响 `L`。所以：

```
∂L/∂w1 = (∂L/∂ŷ) × (∂ŷ/∂z2) × (∂z2/∂a1) × (∂a1/∂z1) × (∂z1/∂w1)
```

**沿着前向的箭头反着走一遍，每一段的导数乘起来。** 这就是"反向"传播这个名字的由来。

### 需要的三个求导结论

```
1. 损失对预测：  L = ½(ŷ-y)²      →  ∂L/∂ŷ = ŷ - y
2. sigmoid：     a = σ(z)         →  ∂a/∂z = a × (1-a)     ← 直接复用前向算好的 a，很省事
3. 线性层：      z = w·a + b      →  ∂z/∂w = a,  ∂z/∂b = 1,  ∂z/∂a = w
```

第 2 条特别值得注意：**sigmoid 的导数可以用它自己的输出算出来**，不用重新算指数。这是它当年流行的一个原因。

### 逐层往回算

**输出层：**

```
∂L/∂ŷ  = ŷ - y = 0.6220 - 1 = -0.3780
∂ŷ/∂z2 = ŷ(1-ŷ) = 0.6220 × 0.3780 = 0.2351

δ2 = ∂L/∂z2 = -0.3780 × 0.2351 = -0.0889     ← δ 读作 delta，叫"误差项"

∂L/∂w2 = δ2 × a1 = -0.0889 × 0.6225 = -0.0553
∂L/∂b2 = δ2                          = -0.0889
```

**隐藏层：**

```
∂L/∂a1 = δ2 × w2 = -0.0889 × 0.8 = -0.0711     ← 梯度穿过 w2 传回来
∂a1/∂z1 = a1(1-a1) = 0.6225 × 0.3775 = 0.2350

δ1 = -0.0711 × 0.2350 = -0.0167

∂L/∂w1 = δ1 × x = -0.0167 × 1.0 = -0.0167
∂L/∂b1 = δ1                     = -0.0167
```

### 更新参数（学习率 0.5）

```
w2 = 0.8 - 0.5 × (-0.0553) = 0.8277
b2 = 0.0 - 0.5 × (-0.0889) = 0.0445
w1 = 0.5 - 0.5 × (-0.0167) = 0.5084
b1 = 0.0 - 0.5 × (-0.0167) = 0.0084
```

再跑一次前向传播，损失会比 0.0714 更小。重复几千次，模型就学会了。

### 从这组数字能看出的规律

**规律一：每层的公式长得一模一样**

```
该层参数梯度 = δ × 该层的输入
下一层的 δ   = (上一层的 δ × 权重) × 本层激活函数的导数
```

正因为这么规整，才能写成通用代码套在任意深的网络上。

**规律二：梯度在往回传的路上会变小**

```
δ2 = -0.0889
δ1 = -0.0167       ← 只有 δ2 的 19%，仅仅穿过一层
```

原因是 sigmoid 的导数 `a(1-a)` 最大只有 **0.25**（在 a=0.5 时）。每往回穿一层就要乘一次这个小数：

```
10 层之后：0.25^10 ≈ 0.00000095
```

前面的层几乎收不到梯度，等于**不学习**了。这就是著名的**梯度消失（Vanishing Gradient）**，也是深度网络曾经训不动的根本原因。

**两个解法**：

- **ReLU**：导数在正半轴恒为 1，乘多少次都不衰减
- **残差连接（ResNet）**：给梯度开一条直通高速路，跳过中间层

理解了这一点，你就理解了 2012 年之后深度学习爆发的一半原因。

---

## 12. 完整训练循环

把所有环节串起来，训练一个神经网络就是这个循环：

```
准备：随机初始化所有 w 和 b
      （不能全设成 0，否则所有神经元完全一样，学不出东西）

重复 N 次：
  ┌─ 1. 前向传播：把一批数据从输入算到输出，得到预测值
  │  2. 算损失：预测值 vs 真实答案，得到一个数字
  │  3. 反向传播：用链式法则算出每个参数的梯度
  │  4. 更新参数：参数 = 参数 - 学习率 × 梯度
  └─ 5. 回到第 1 步

结束：损失不再明显下降，或者达到设定的轮数
```

**就这五步，没有第六步。** 从最简单的线性回归到 GPT，训练流程完全一致。

### 关于"批量"

一次喂多少数据进去？

| 方式 | 一次用多少条 | 特点 |
|---|---|---|
| 全量（BGD） | 全部 | 梯度最准，但内存吃不消 |
| 随机（SGD） | 1 条 | 快，但方向抖动大 |
| **小批量（Mini-batch）** | 32 ~ 256 条 | **实际标准做法**，兼顾稳定和速度 |

**一个 epoch = 全部训练数据被完整过了一遍。** 假设有 10000 条数据、batch size 是 100，那么一个 epoch 包含 100 次参数更新。

---

## 13. 可运行代码

### 版本一：纯 NumPy 手写（推荐先读这个）

不依赖任何框架，每一行都对应上面讲过的公式。解决的正是第 7 节那个"直线学不会"的异或问题。

```python
import numpy as np

np.random.seed(42)

# ============ 数据：异或问题 ============
X = np.array([[0, 0],
              [0, 1],
              [1, 0],
              [1, 1]])          # 4 个样本，每个 2 个特征
y = np.array([[0], [1], [1], [0]])   # 相同为 0，不同为 1

# ============ 网络结构：2 → 4 → 1 ============
# 随机初始化。乘 0.5 是为了让初始值小一些，避免一开始就落进 sigmoid 的饱和区
W1 = np.random.randn(2, 4) * 0.5     # 第一层权重
b1 = np.zeros((1, 4))                # 第一层偏置
W2 = np.random.randn(4, 1) * 0.5     # 第二层权重
b2 = np.zeros((1, 1))

lr = 0.5          # 学习率

def sigmoid(z):
    return 1 / (1 + np.exp(-z))

# ============ 训练循环 ============
for epoch in range(10000):
    # ---------- 1. 前向传播 ----------
    z1 = X @ W1 + b1          # 加权求和，@ 是矩阵乘法
    a1 = sigmoid(z1)          # 激活
    z2 = a1 @ W2 + b2         # 第二层加权求和
    y_hat = sigmoid(z2)       # 最终预测值

    # ---------- 2. 算损失 ----------
    loss = np.mean((y_hat - y) ** 2) / 2

    # ---------- 3. 反向传播 ----------
    # 输出层：∂L/∂z2 = (ŷ-y) × σ'(z2)，其中 σ'(z2) = ŷ(1-ŷ)
    delta2 = (y_hat - y) * y_hat * (1 - y_hat)
    dW2 = a1.T @ delta2 / len(X)      # 该层梯度 = 该层输入的转置 × δ
    db2 = delta2.mean(axis=0, keepdims=True)

    # 隐藏层：梯度先穿过 W2 传回来，再乘本层激活函数的导数
    delta1 = (delta2 @ W2.T) * a1 * (1 - a1)
    dW1 = X.T @ delta1 / len(X)
    db1 = delta1.mean(axis=0, keepdims=True)

    # ---------- 4. 更新参数 ----------
    W2 -= lr * dW2            # 沿梯度的反方向走一小步
    b2 -= lr * db2
    W1 -= lr * dW1
    b1 -= lr * db1

    if epoch % 2000 == 0:
        print(f"epoch {epoch:5d}  loss = {loss:.6f}")

# ============ 看结果 ============
print("\n预测结果：")
for i in range(4):
    print(f"  输入 {X[i]}  →  预测 {y_hat[i][0]:.4f}  (答案 {y[i][0]})")
```

**预期输出**：四个预测值分别接近 0、1、1、0。**一条直线做不到的事，两层网络做到了。**

把隐藏层去掉（直接 `X @ W2 + b2`）再跑一次，你会看到损失卡在 0.125 左右下不去 —— 亲手验证一遍第 7 节的结论。

### 版本二：PyTorch（实际工作中的写法）

```python
import torch
import torch.nn as nn

X = torch.tensor([[0., 0.], [0., 1.], [1., 0.], [1., 1.]])
y = torch.tensor([[0.], [1.], [1.], [0.]])

# 网络结构，和上面完全等价
model = nn.Sequential(
    nn.Linear(2, 4),      # 全连接层：2 个输入 → 4 个神经元
    nn.Sigmoid(),         # 激活函数
    nn.Linear(4, 1),
    nn.Sigmoid(),
)

criterion = nn.MSELoss()                              # 损失函数
optimizer = torch.optim.SGD(model.parameters(), lr=0.5)  # 优化器

for epoch in range(10000):
    y_hat = model(X)              # 1. 前向传播
    loss = criterion(y_hat, y)    # 2. 算损失

    optimizer.zero_grad()         # 清空上一轮的梯度（不清会累加）
    loss.backward()               # 3. 反向传播 —— 框架自动求所有梯度
    optimizer.step()              # 4. 更新参数

    if epoch % 2000 == 0:
        print(f"epoch {epoch:5d}  loss = {loss.item():.6f}")

print(model(X))
```

**对照着看这两份代码**：PyTorch 版把第 11 节那一堆链式法则的手工计算，压缩成了 `loss.backward()` 一行。框架帮你做的就是**自动微分** —— 它记录了前向的每一步操作，然后自动反着求一遍导数。

> **建议**：先把 NumPy 版跑通、改改参数玩一玩，再用框架。否则框架对你就是黑盒，出了问题无从下手。

---

## 14. 黑话词典

| 术语 | 人话解释 |
|---|---|
| **特征 Feature** | 输入的每一个字段（面积、房龄、楼层） |
| **标签 Label** | 真实答案，监督学习的数据必须带 |
| **样本 Sample** | 一条数据（一行） |
| **参数 Parameter** | 模型自己学出来的 w 和 b |
| **超参数 Hyperparameter** | 你手动设的，比如学习率、层数、batch size。模型不会自己学这些 |
| **Epoch** | 全部训练数据完整过一遍 |
| **Batch Size** | 一次喂给模型多少条数据 |
| **迭代 Iteration** | 一次参数更新 |
| **过拟合 Overfitting** | 把训练数据背下来了，见到新数据就傻眼。表现：训练集准确率 99%，测试集 60% |
| **欠拟合 Underfitting** | 连训练数据都学不好，模型太简单了 |
| **正则化 Regularization** | 防过拟合的手段总称（L2、Dropout、早停） |
| **Dropout** | 训练时随机"关闭"一部分神经元，强迫网络不依赖某几个 |
| **归一化 Normalization** | 把不同量纲的特征缩放到同一范围。**不做这步训练会非常慢** |
| **训练集/验证集/测试集** | 一般 6:2:2 划分。测试集只在最后用一次，用多了就等于作弊 |
| **优化器 Optimizer** | 更新参数的策略。SGD 是最基础的，Adam 是现在的默认选择 |
| **梯度消失** | 梯度往回传的路上越来越小，前面的层学不动 |
| **梯度爆炸** | 反过来，梯度越来越大直接溢出成 NaN。解法：梯度裁剪 |

### 三个新手最常踩的坑

**1. 忘记做特征归一化**

如果特征 A 的范围是 0~1、特征 B 是 0~10000，损失函数的等高线会被拉成极扁的椭圆，梯度方向几乎垂直于最优方向，来回横跳。

```python
X = (X - X.mean(axis=0)) / X.std(axis=0)   # 标准化，一行的事，收敛快一个数量级
```

**2. 用测试集调参**

反复用测试集选超参数，等于间接把测试集信息泄露给了模型，你看到的分数是虚高的。**测试集只能用一次。** 调参要用验证集。

**3. 参数全初始化为 0**

所有神经元的输入输出完全一样，梯度也完全一样，永远学不出差异 —— 这叫**对称性问题**。必须随机初始化。

---

## 15. 下一步学什么

### 建议路线

```
第一步（现在这份文档）
  └─ 梯度下降 + 前向 / 反向传播 + 手写一个小网络

第二步：把基本功补齐
  ├─ 分类问题：Softmax + 交叉熵损失（比 MSE 更适合分类）
  ├─ 更好的激活函数：ReLU 及其变种
  ├─ 更好的优化器：Momentum → RMSProp → Adam
  └─ 防过拟合：Dropout、L2 正则、早停

第三步：挑一个方向深入
  ├─ CNN 卷积神经网络    → 图像
  ├─ RNN / LSTM         → 序列（现在多被 Transformer 替代）
  └─ Transformer        → 文本、大模型，当前主流

第四步：动手做完整项目
  └─ MNIST 手写数字识别 → CIFAR-10 图像分类 → 自己找数据做一个
```

### 学习方法建议

**动手 > 看视频。** 每学一个概念，立刻改代码验证。比如：

- 把学习率从 0.5 改成 5，看损失怎么爆炸
- 把隐藏层删掉，看异或问题怎么学不会
- 把 sigmoid 换成 ReLU，看收敛快多少
- 把初始化全改成 0，看模型怎么原地不动

**这些"故意做错"的实验，比读十篇教程有用。**

### 一句话收尾

> 神经网络不神秘：
> **它就是一堆"乘加 + 压缩"的组合，靠不断算"往哪个方向改能少错一点"，一小步一小步地调出来。**
>
> 你已经手算过一遍完整的前向和反向传播了 —— 剩下的所有东西，都是在这个骨架上加零件。

---

*本文所有数字都可以用计算器复现。如果哪一步对不上，请回到那一节重新算一遍，不要跳过。*
