# 神经网络进阶：RNN 与 LSTM（序列方向）

> 承接《[从机器学习到神经网络：零基础入门](从机器学习到神经网络-零基础入门.md)》第 15 节"第三步"。
> 这一篇挑的第二个方向是 **RNN（循环神经网络）与它的改进版 LSTM**，专门处理有先后顺序的数据：一句话、一段股价、一段音频。

---

## 目录

1. [为什么普通网络处理不了序列](#1-为什么普通网络处理不了序列)
2. [RNN：带一个"记忆"的神经元](#2-rnn带一个记忆的神经元)
3. [手算一遍 RNN 前向传播](#3-手算一遍-rnn-前向传播)
4. [BPTT：沿着时间反向传播](#4-bptt沿着时间反向传播)
5. [RNN 的老毛病：梯度消失 / 爆炸](#5-rnn-的老毛病梯度消失--爆炸)
6. [LSTM：三个门解决"记忆"问题](#6-lstm三个门解决记忆问题)
7. [GRU：LSTM 的简化版](#7-gru-lstm-的简化版)
8. [完整代码：用 LSTM 做序列分类](#8-完整代码用-lstm-做序列分类)
9. [黑话词典](#9-黑话词典)
10. [检查清单](#10-检查清单)

---

## 1. 为什么普通网络处理不了序列

上一篇 CNN 处理的是"空间"结构（图像的上下左右），这一篇要处理"时间"结构：一句话里词的先后顺序会改变意思——

```
"狗咬人"  和  "人咬狗"      ← 完全相同的三个字，顺序不同意思天差地别
```

全连接网络或 CNN 有两个问题应付不了这种数据：

- **输入长度不固定**：一句话可能 5 个字，也可能 50 个字，全连接网络的输入层大小是固定的
- **不记得"之前发生了什么"**：网络需要一边读一边记住前面读到的内容，才能理解后面的词

**RNN 的核心思路**：维护一个"记忆"（隐藏状态），每读入一个新的词，就用"新词 + 之前的记忆"一起算出"新的记忆"，一步一步滚下去。

---

## 2. RNN：带一个"记忆"的神经元

### 结构

```
        h0 ──┐
             ▼
x1 ──→ [ RNN 单元 ] ──→ h1 ──┐
                             ▼
x2 ──→ [ RNN 单元 ] ──→ h2 ──┐
                             ▼
x3 ──→ [ RNN 单元 ] ──→ h3 ──→ ...
```

**每一步用的都是同一套参数**（权重共享，和 CNN 的卷积核共享是同一种思想），只是每一步的输入变成了"当前词 + 上一步留下的记忆"。

### 公式

```
h_t = tanh( Wx·x_t + Wh·h_(t-1) + b )
```

- `x_t`：第 t 步的输入（比如第 t 个词的向量）
- `h_(t-1)`：上一步留下的"记忆"（隐藏状态）
- `h_t`：这一步算出的新记忆，会同时传给下一步，也可以用来做当前步的输出预测

**这本质上还是"加权求和 + 激活"**（回忆入门笔记第 8 节的神经元结构），只是多了一路输入来自"自己上一步的输出"。

---

## 3. 手算一遍 RNN 前向传播

用一个最小的例子：输入是标量（1 维），隐藏状态也是 1 维。

```
参数：Wx = 0.5,  Wh = 0.8,  b = 0
初始记忆：h0 = 0
输入序列：x1 = 1.0,  x2 = 2.0
```

**第 1 步：**

```
h1 = tanh(0.5×1.0 + 0.8×0 + 0) = tanh(0.5) = 0.4621
```

**第 2 步（关键：这一步用到了 h1，也就是第 1 步的"记忆"）：**

```
h2 = tanh(0.5×2.0 + 0.8×0.4621 + 0)
   = tanh(1.0 + 0.3697)
   = tanh(1.3697)
   = 0.8788
```

**看这里**：如果没有 `0.8 × h1` 这一项，`h2` 只会由 `x2` 单独决定，网络会"忘记"第一步看到的 `x1=1.0`。正是这一项让 RNN "记住了之前发生的事"。

---

## 4. BPTT：沿着时间反向传播

RNN 的反向传播叫 **BPTT（Backpropagation Through Time）**，本质上还是入门笔记第 11 节的链式法则，只是这次"层"变成了"时间步"——把 RNN 按时间展开，看起来就是一个"每一层参数都相同"的很深的网络：

```
展开后：
x1 → [RNN] → h1 → [RNN] → h2 → [RNN] → h3 → ... → 输出 → 损失
              ↑ x2 加入        ↑ x3 加入
```

梯度要从最后的损失，沿着这条链一路乘回到最早的 `h1`，跟前面手推 sigmoid 反向传播时"一路乘导数"的做法完全一样。

---

## 5. RNN 的老毛病：梯度消失 / 爆炸

序列一旦变长（比如 100 个词），梯度要连续乘 100 次 `tanh` 的导数和 `Wh`：

```
tanh 的导数最大只有 1（在 0 附近），大部分时候小于 1
如果 |Wh| 也小于 1，梯度会以指数级衰减 → 梯度消失，网络记不住很久以前的词
如果 |Wh| 大于 1，梯度会以指数级放大 → 梯度爆炸，损失变成 NaN
```

这跟第一篇入门笔记第 11 节讲的**梯度消失**是同一个数学根源（连乘小于 1 的数），只是这次"深度"不是网络的层数，而是**序列的长度**。

**朴素 RNN 记不住长距离依赖**——比如一句很长的话，开头提到的主语，到句尾做动词一致性判断时早就被"忘光"了。这正是 LSTM 要解决的问题。

---

## 6. LSTM：三个门解决"记忆"问题

**LSTM（Long Short-Term Memory）** 在隐藏状态之外，又加了一条**细胞状态（cell state）** `C_t`，专门负责"长期记忆"，并用三个"门"（本质是 sigmoid 输出的 0~1 之间的数，当"开关"用）来控制信息怎么流动。

```
遗忘门 f_t = σ(Wf·[h_(t-1), x_t] + bf)     ← 决定"旧记忆"要保留多少（0=完全忘记，1=完全保留）
输入门 i_t = σ(Wi·[h_(t-1), x_t] + bi)     ← 决定"新信息"要写入多少
候选值 C̃_t = tanh(Wc·[h_(t-1), x_t] + bc)  ← 这一步"想要"写入的新内容

细胞状态更新：
C_t = f_t × C_(t-1) + i_t × C̃_t
       ↑ 保留的旧记忆    ↑ 写入的新记忆

输出门 o_t = σ(Wo·[h_(t-1), x_t] + bo)     ← 决定这一步要"说出口"多少记忆
h_t = o_t × tanh(C_t)
```

### 直觉类比：一个会做笔记的学生

- **遗忘门**：翻笔记本，划掉不再重要的旧内容
- **输入门 + 候选值**：把这一句话里的新信息，按"重要程度"记到笔记本上
- **细胞状态**：笔记本本身，可以一直往后传，只要门不主动删，内容能保留很久
- **输出门**：需要回答问题时，从笔记本里挑出跟当前问题相关的部分说出来

**为什么这样能缓解梯度消失**：细胞状态 `C_t` 的更新是"加法"（`f_t × C_(t-1) + i_t × C̃_t`），而不是普通 RNN 里反复做"矩阵乘法 + tanh"，加法这条路径**梯度可以几乎无衰减地一路传回去**（只要遗忘门 `f_t` 不主动关掉），这正是 LSTM 能记住长距离依赖的数学原因。

---

## 7. GRU：LSTM 的简化版

**GRU（Gated Recurrent Unit）** 把 LSTM 的三个门砍成两个（**更新门**、**重置门**），也不再单独维护细胞状态，直接用隐藏状态兼任。参数更少、计算更快，很多任务上效果和 LSTM 不相上下。

| | 门的数量 | 状态 | 参数量 | 速度 |
|---|---|---|---|---|
| **LSTM** | 3（遗忘/输入/输出） | 隐藏状态 + 细胞状态 | 较多 | 较慢 |
| **GRU** | 2（更新/重置） | 只有隐藏状态 | 较少 | 较快 |

**经验规则**：数据量小或者要跑得快，先试 GRU；序列特别长、任务对"精确记住很久以前的信息"要求高，用 LSTM。

---

## 8. 完整代码：用 LSTM 做序列分类

一个把变长数字序列分类成"递增"或"非递增"的小任务：

```python
import torch
import torch.nn as nn

class SeqClassifier(nn.Module):
    def __init__(self, input_size=1, hidden_size=16, num_classes=2):
        super().__init__()
        self.lstm = nn.LSTM(input_size, hidden_size, batch_first=True)
        self.fc = nn.Linear(hidden_size, num_classes)

    def forward(self, x):
        # x 形状: (batch, 序列长度, 每步特征数)
        outputs, (h_n, c_n) = self.lstm(x)
        # h_n 是最后一个时间步的隐藏状态，浓缩了整个序列的信息
        last_hidden = h_n[-1]              # 取最后一层的隐藏状态
        logits = self.fc(last_hidden)      # 交给全连接层做分类
        return logits

model = SeqClassifier()
criterion = nn.CrossEntropyLoss()          # 复用第二篇的 Softmax + 交叉熵
optimizer = torch.optim.Adam(model.parameters(), lr=1e-3)

# 假数据：8 条长度为 5 的序列，每步 1 个特征
x = torch.randn(8, 5, 1)
y = torch.randint(0, 2, (8,))

logits = model(x)
loss = criterion(logits, y)
loss.backward()
optimizer.step()
print("输出形状:", logits.shape, " 损失:", loss.item())
```

`nn.LSTM` 内部自动实现了第 6 节的三个门和细胞状态更新，你不需要手写；`h_n[-1]` 就是"读完整个序列后留在脑子里的最终记忆"，拿它去做分类正是 RNN/LSTM 处理序列任务的标准套路。

---

## 9. 黑话词典

| 术语 | 人话解释 |
|---|---|
| **隐藏状态 Hidden State** | RNN 每一步维护、传递给下一步的"记忆" |
| **细胞状态 Cell State** | LSTM 专门维护长期记忆的一条状态，更新方式接近"加法"而非"乘法" |
| **门 Gate** | 一个输出 0~1 之间数值的 sigmoid，用来控制信息流动的比例 |
| **BPTT** | 沿时间维度展开后做反向传播，本质仍是链式法则 |
| **长距离依赖 Long-range Dependency** | 序列里相隔很远的两个位置之间的依赖关系，普通 RNN 难以捕捉 |
| **GRU** | LSTM 的简化版，两个门、无独立细胞状态 |
| **Seq2Seq** | 输入一个序列，输出另一个序列的框架（如机器翻译），常用 RNN/LSTM 作为编码器解码器 |

---

## 10. 检查清单

- [ ] 能说出全连接/CNN 处理序列数据时的两个问题
- [ ] 能手算一遍 2 步的 RNN 前向传播
- [ ] 理解 BPTT 为什么本质上还是链式法则
- [ ] 能解释 RNN 的梯度消失/爆炸和入门笔记第 11 节的联系
- [ ] 能画出 LSTM 三个门的作用，并用"做笔记的学生"类比讲给别人听
- [ ] 知道 GRU 和 LSTM 的主要区别
- [ ] 能读懂第 8 节的代码，说出 `h_n[-1]` 代表什么

打勾之后，继续挑战本篇提到"现在多被 Transformer 替代"——下一篇就讲 Transformer。

---

*建议动手改一下第 3 节的数字，重新算一遍 h2，体会"记忆"是怎么被继承下来的。*
